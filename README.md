# Full Stack Boilerplate

## Work with it

`cd backend` and `uvicorn main:app --reload` to spin up the backend server

`cd fronend` and `npm start` to spin up the react frontend server with hot reload

### Coming Soon

A fully dockerised application
